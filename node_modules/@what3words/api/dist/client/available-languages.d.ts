import { ApiClient } from '../lib';
import type { ApiClientConfiguration, Transport } from '../lib';
export interface AvailableLanguagesResponse {
    languages: {
        code: string;
        name: string;
        nativeName: string;
    }[];
}
export declare class AvailableLanguagesClient extends ApiClient<AvailableLanguagesResponse> {
    protected readonly method = "get";
    protected readonly url = "/available-languages";
    static init(apiKey?: string, config?: ApiClientConfiguration, transport?: Transport): AvailableLanguagesClient;
}
